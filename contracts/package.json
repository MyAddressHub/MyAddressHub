{
  "name": "myaddresshub-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for MyAddressHub",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:localhost": "hardhat run deploy.js --network localhost",
    "deploy:polygon": "hardhat run deploy.js --network polygon",
    "deploy:mumbai": "hardhat run deploy.js --network mumbai",
    "test": "hardhat test",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "hardhat": "^2.17.0"
  },
  "dependencies": {
    "ethers": "^6.8.1"
  }
}
